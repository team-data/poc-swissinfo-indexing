nelmio_solarium:
    endpoints:
        default:
            host: "%env(SOLR_HOST)%"
            core: "%env(SOLR_CORE)%"
            port: "%env(SOLR_PORT)%"
            # The following are the default parameters for Solarium client:
            # scheme: http
            # port: 8983
            # path: /solr
            # timeout: 5
    clients:
        default:
            endpoints: [default]
